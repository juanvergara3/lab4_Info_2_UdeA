#ifndef FUNCTIONS_H
#define FUNCTIONS_H
#include <string>
#include <limits>
#include <iostream>

bool is_number(std::string s);

int get_cost(bool can_be_negative);

short menu_selection(std::string menu);

#endif // FUNCTIONS_H
